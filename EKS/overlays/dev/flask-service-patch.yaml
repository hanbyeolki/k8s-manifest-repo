apiVersion: v1
kind: Service
metadata:
  name: team3-flask
  namespace: team3
  annotations:
    alb.ingress.kubernetes.io/healthcheck-path: "/"
  labels:
    env: dev
spec:
  selector:
    app: team3-flask