apiVersion: v1
kind: Service
metadata:
  name: team3-flask
  namespace: team3
  labels:
    env: dev
spec:
  selector:
    app: team3-flask
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80